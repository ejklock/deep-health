# Test fixture: minimal sonar-project.properties for integration tests.
# Real user files live at the project root — this copy is consumed by the
# orchestrator integration tests that exercise the SonarQube engine.
sonar.projectKey=test-project
sonar.projectName=Test Project
sonar.sources=./
sonar.host.url=http://localhost:9000
